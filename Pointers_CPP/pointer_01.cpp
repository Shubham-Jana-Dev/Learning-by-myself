#include <iostream>
#include <vector>

int main() {
    // Pointer is a hexadecimal number that represent the 
    // address where variable is troring its data.

    int a = 2036;
    std::cout<<"the address of a is " << &a <<std::endl;
    return 0;
}